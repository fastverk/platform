# platform
One git vehicle for fastverk gateway/adapter modules (forge, tracker, service-finder, wave). Independently versioned Bazel modules; tags are &lt;module>/vX.Y.Z (not lockstep). Public contracts live in fastverk/contracts.
