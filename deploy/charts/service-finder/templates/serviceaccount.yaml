{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "service-finder.serviceAccountName" . }}
  labels:
    {{- include "service-finder.labels" . | nindent 4 }}
{{- end }}
